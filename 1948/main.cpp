#include <stdio.h>

using namespace std;

int main(){
    int N, M;
    scanf("%d", &N);
    scanf("%d", &M);
    int input[3];
    for(int i = 0; i< M; i++){
        scanf("%d %d %d", input, input+1, input+2);
        //printf("%d %d %d\n", input[0], input[1], input[2]);
        
    }
    int start, end;
    scanf("%d %d", &start, &end);
    
    return 0;
}